defaults:
  - _self_
  - initializer: kaiming
  - model: synaps-delay
  - input: input-group-delay
  - neuron: custom-lif-group
  - readout: custom-readout
  - training: training
  - plotting: plotting
  - connection: connection
  - connection_delay: connection-delay

name: synaps-delay

nb_hidden: 1
hidden_size: [64]
recurrent: [True] 

multiple_readouts: False

is_half: True

compute_nu: True

stochastic: False

nb_workers: 2

# Maximum Delay
max_delays: 0.1 # in seconds

device: "cuda"